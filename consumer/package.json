{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "compile": "tsc",
    "postinstall": "npm run compile",
    "start": "ts-node-dev --poll --respawn --transpile-only index.ts"
  },
  "dependencies": {
    "kafkajs": "^2.2.2",
    "ts-node-dev": "^2.0.0"
  }
}
